{
  "permissions": {
    "allow": [
      "Exec(find)",
      "Exec(curl)",
      "Exec(mvn)",
      "Exec(sleep)",
      "Exec(npm run)",
      "Exec(cp)",
      "Exec(rm)",
      "Exec(netstat)",
      "Exec(taskkill)",
      "Exec(findstr)",
      "Exec(timeout)",
      "Exec(npm install)",
      "Exec(git status)",
      "Exec(git init)",
      "Exec(psql)",
      "Exec(docker)",
      "Exec(head)",
      "Exec(git add)"
    ]
  }
}